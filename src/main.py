import os
import src.config as config
import src.cvision as cv

# Get dumped hist data
vision = cv.CVision()

# Get the query image (currently from directory, but need to convert to web service)
# call matcher 1
# call matcher 2
# call matcher 3

# select best match

# scrap data

# return data (send data via web service)
